{
  "name": "@familietools/module-package",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check:test": "tsc -p tsconfig.test.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@familietools/module-schema": "workspace:*",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^6.0.0",
    "vitest": "^5.0.1"
  }
}
